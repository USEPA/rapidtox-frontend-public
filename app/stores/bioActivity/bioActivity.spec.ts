import {
  describe,
} from 'vitest';

describe.todo('BioActivity Store Unit Tests');

// import {setActivePinia, createPinia} from 'pinia';
// import {useBioactivityStore} from './bioActivity';

// describe('Bioactivity Store', () => {
//   beforeEach(() => {
//     setActivePinia(createPinia());
//   });

/* Test Bioactivity summary(subtab) - chems show up properly

*/

/* Test Toxcast(subtab) selections, nested (dtxsid) selections */

/* Test SEEM3 BER Summary - this will be most difficult section to test. Need to test BER calculator, formula calculations,
   BER inputs and calculations are propagating properly to the gird, the correct chemicals from search are propagating
   and synched up (if user removed from selections the SEEM3 should be reflected),
  */
/* Test session load */
// })
